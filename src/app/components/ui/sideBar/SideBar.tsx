export default function SideBar() {
  return (
    <>
      <nav className="flex flex-col h-[calc(100vh-152px)] w-96 bg-green-400">
        <div> is menu</div>
        <div> is menu</div>
        <div> is menu</div>
        <div> is menu</div>
      </nav>
    </>
  )
}